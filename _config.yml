title: Underthink
description: "Your best thinking happens when you're not trying. The science of showers, walks, and letting your mind work."
url: "https://underthink.app"
baseurl: ""

# Disable the default theme to allow our custom CSS to take over
theme: []

markdown: kramdown
kramdown:
  input: GFM

permalink: /:slug/

sass:
  style: compressed

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - LICENSE

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

author:
  name: "Laurence Mayhead"
